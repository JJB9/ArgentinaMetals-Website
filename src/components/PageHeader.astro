---
interface Props {
  title: string;
  text: string;
}

const { title, text } = Astro.props;
---

<section class="page-hero">
  <div class="wrap">
    <h1>{title}</h1>
    <p>{text}</p>
  </div>
</section>
